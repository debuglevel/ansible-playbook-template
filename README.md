# load-balanced BigBlueButton testing environment

## Requirements
* Vagrant 
* VirtualBox
* host with enough ressources

## Vagrant
Use `vagrant up` to start virtual machines on which the BBB installation will be done.